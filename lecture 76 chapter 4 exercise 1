#lecture 76 oops concept
#inheretence
class Phone:
    def _init_(self,brand,model_name,price):
        self.brand = brand
        self.model_name = model_name
        self.price = max(price,0)

def full_name(self):
    return f"{self.brand} {self.model_name}"
    
def make_a_call(self,number):
    return f"calling {number}...."
    
class smartPhone:
    def _init_(self, brand,model_name, price, ram, internal_memory, rear_camera):
        self.brand = brand
        self.model_name = model_name
        self.price = max(price,0)
        self.ram = ram
        self.internal_memory = internal_memory
        self.rear_camera = rear_camera

def full_name(self):
    return f"{self.brand} {self.model_name}"
    
def make_a_call(self,number):
    return f"calling {calling}...."
def make_a_call(self,number):
    return f"calling {nuumber}...."
    
    
phone = phone('nokia', '1100', '1000')
smartPhone = smartPhone('oneplus', '5', '3000', '6 GB', '20MP')
print(phone.full_name())
print(smartphone.full_name())
